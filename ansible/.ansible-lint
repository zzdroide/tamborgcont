skip_list:
  - no-handler        # https://github.com/ansible/ansible-lint/issues/419
  - role-name[path]   # This won't be published to Galaxy

profile: production

exclude_paths:
  - .cache/
  - .galaxy/
